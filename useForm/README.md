# UseForm

Ejemplo 
```
    const initialState = {
        name  : 'name',
        edad : 0,
        email : '...@gmail'
    }
    const [ values, handleInputChange, reset ] = useForm(initialState);
```

// recibe un argumento inicial 